"use strict"

//alert( 3 + 3) // since we are using node not browser so we got --> ReferenceError: alert is not defined

let num = 123 // number --> 2 to power 53 range-> bigint

let city = "nainital"  // string

let userLogedIn = true // boolean

let phone = null // standalone value ,  explicitly stating that the variable does not point to any object or value, but it is expected to in the future or has been intentionally cleared
typeof null // is object

let user; // undefined type is undefined

// symbol => unique

// object